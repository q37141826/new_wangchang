<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- This is the line -->
    <!-- left 和right是左右边距 -->
    <item
      >
        <shape>
            <solid android:color="@color/white"  />
        </shape>
    </item>
    <!--bottom是横线高度 -->
    <item
        android:left="@dimen/margin50dp"
        android:right="@dimen/margin50dp"
       android:bottom="@dimen/margin2dp">
       >
        <shape>
            <solid android:color="@color/theme_color" />
        </shape>
    </item>
    <!--bottom是横线高度 -->
    <item android:bottom="@dimen/margin4dp">
        <shape>
            <solid android:color="@color/white" />
        </shape>
    </item>

</layer-list>